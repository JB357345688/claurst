# TASK-M7-04 Review

## Preflight
- Ticket id: `TASK-M7-04`
- Verdict: review basis verified, but not scope-clean for closure
- Verified files/symbols/commands:
  - `AGENTS.md`
  - `docs/Current/MPWO_WORK_ORDER_PACK.md` (`TASK-M7-04`)
  - `src-rust/crates/query/src/lib.rs`
  - `src-rust/crates/query/src/provider_resolution.rs`
  - `git diff --name-status a09b3daefe887f2794c9fc2154afd8ebc8b3ec27`
  - `cargo check -p claurst-query`
  - `cargo check --workspace`
- Drift found:
  - Accepted baseline commit `a09b3daefe887f2794c9fc2154afd8ebc8b3ec27` is the current `HEAD`, so the review basis is the active working-tree delta from that baseline.
  - That delta is not limited to `TASK-M7-04`; it also includes tracked changes outside the ticket file set.
- Blockers:
  - Unrelated tracked changes are present in the review basis:
    - `src-rust/crates/api/src/registry.rs`
    - `RFC_PROVIDER_AWARE_WORKER_FABRIC.md`
    - `RFC_PROVIDER_AWARE_WORKER_FABRIC_v2.md`

## Review
- Pass/fail: `FAIL`
- Exact violations:
  - Scope compliance failed. `TASK-M7-04` is defined to modify `src-rust/crates/query/src/lib.rs`, but the delta from the accepted baseline also contains unrelated tracked changes in `src-rust/crates/api/src/registry.rs` and deletions of `RFC_PROVIDER_AWARE_WORKER_FABRIC.md` and `RFC_PROVIDER_AWARE_WORKER_FABRIC_v2.md`. The active review patch is therefore not scope-clean.
- Minimal corrective actions:
  - Remove the unrelated tracked changes from the review basis for this ticket, or split/restack them so the `TASK-M7-04` closure patch contains only the `src-rust/crates/query/src/lib.rs` seam delta.
- Ticket checks completed without additional violations:
  - `run_query_loop()` now calls `provider_resolution::resolve_provider_identity()` at `src-rust/crates/query/src/lib.rs:860`.
  - The inline materialization block is replaced by `provider_resolution::materialize_provider()` at `src-rust/crates/query/src/lib.rs:872`.
  - `use_provider_dispatch`, the inline Anthropic filter, `provider_id_str`, `model_id_str`, and the inline `known_providers` array are removed from `lib.rs`.
  - Downstream uses consume `ExecutionTarget` fields inline at `src-rust/crates/query/src/lib.rs:889-965`.
  - Capability shaping remains inline in `lib.rs` at `src-rust/crates/query/src/lib.rs:906-915`.
  - Registry-backed resolution/materialization failures now return `QueryOutcome::Error` at `src-rust/crates/query/src/lib.rs:866-885`, so they do not fall through to the raw Anthropic path.
  - No `TASK-M7-05` test leakage was found in this delta.
- Validations run:
  - `cd src-rust && cargo check -p claurst-query` — passed
  - `cd src-rust && cargo check --workspace` — passed
- Ready to close: `no`
