# PRE_M8_01 Recon Report

- Ticket: `TASK-M8-01` preflight readiness reconnaissance
- Timestamp: `2026-04-12T04:13:06Z`
- Scope: read-only repo scan plus required baseline commands
- Source edits: none
- Artifact change: this report file only

## Branch

- Current branch: `feature/provider-resolution-seam`

## Working Tree Summary

- `git diff --quiet`: clean
- `git diff --cached --quiet`: clean
- `git status --short`: only tolerated untracked noise under `.codex/`, `docs/`, and `src-rust/target/`
- Repo baseline verdict: clean enough committed baseline to begin `M8-01` preflight

## Authority Reviewed

- Reviewed `/home/jordi/claurst/AGENTS.md`
- Reviewed `/home/jordi/claurst/docs/Current/MPWO_WORK_ORDER_PACK.md`
- Confirmed from the pack:
  - all M8 worker-propagation work requires `M7-07` complete
  - `M8-01` adds `provider_registry` and `model_registry` to `ToolContext`
  - `M8-02` depends on `M8-01`
  - `M8-03` has no M8-local preconditions and can proceed in parallel with `M8-01/M8-02`
  - `M8-04` and `M8-05` both depend on `M8-01` plus `M8-03`
  - the standing hosted-Ollama non-regression invariant remains binding for remaining seam work

## M8 Dependency Confirmation

- Pack dependency chain confirms:
  - `M8-01 -> M8-02`
  - `M8-01 -> M8-04`
  - `M8-01 -> M8-05`
  - `M8-03 -> M8-04`
  - `M8-03 -> M8-05`
- Practical read: `M8-03` is independently ready, while `M8-04/M8-05` cannot proceed safely until both the `ToolContext` registry seam and the `AgentInput.provider` seam exist.

## ToolContext Construction-Site Inventory

- `ToolContext` is defined at `src-rust/crates/tools/src/lib.rs:216`
- Repo-wide construction-site count: `3`
- Exact file count: `2`
- Escalation threshold check (`>10` sites): not exceeded
- Helper/factory wrappers found: none

| File | Approx line | Classification | Notes |
|---|---:|---|---|
| `src-rust/crates/cli/src/main.rs` | 651 | Production construction site | Root startup `tool_ctx` literal |
| `src-rust/crates/tools/src/lib.rs` | 539 | Test-only construction site | `test_resolve_path_absolute` fixture |
| `src-rust/crates/tools/src/lib.rs` | 567 | Test-only construction site | `test_resolve_path_relative` fixture |

## Startup Registry Source Tracing

- Root-session `provider_registry` startup source:
  - `src-rust/crates/cli/src/main.rs:612-613`
  - variable name: `provider_registry`
  - expression: `claurst_api::ProviderRegistry::from_environment_with_auth_store(client_config)`
  - type at that point: bare `claurst_api::ProviderRegistry`
- Root-session `provider_registry` sharing/passing:
  - `src-rust/crates/cli/src/main.rs:739`
  - wrapped as `let provider_registry = std::sync::Arc::new(provider_registry);`
  - live shared type after wrap: `Arc<claurst_api::ProviderRegistry>`
  - passed into query runtime at `query_config.provider_registry = Some(provider_registry.clone())` on line 740
- Root-session `model_registry` startup source:
  - `src-rust/crates/cli/src/main.rs:709`
  - variable name: `model_registry`
  - expression: `load_cached_model_registry()`
  - type returned by helper: `Arc<claurst_api::ModelRegistry>` (`src-rust/crates/cli/src/main.rs:852`)
  - passed into query runtime at `query_config.model_registry = Some(model_registry.clone())` on line 714
- `ToolContext` construction site in `main.rs`:
  - `src-rust/crates/cli/src/main.rs:651-662`
  - current fields do not yet include registry slots
- Additional runtime evidence:
  - `QueryConfig` already carries `provider_registry: Option<Arc<ProviderRegistry>>` and `model_registry: Option<Arc<ModelRegistry>>` in `src-rust/crates/query/src/lib.rs:117-128`
  - refresh path already rebuilds both registries as Arcs in `RefreshedProviderRuntime` at `src-rust/crates/cli/src/main.rs:899-965`

### M8-02 Assessment

- Ownership/lifetime risk: low
- Sequencing note: `ToolContext` is constructed before `model_registry` exists, so `M8-02` will need a small startup ordering adjustment even though the registry types themselves are already Arc-friendly.
- Likely implementation shape: delay or relocate the `ToolContext` literal until after `model_registry` and the Arc-wrapped `provider_registry` are available, without changing how either registry is built.
- Conclusion: straightforward with notes, not blocked.

## AgentInput / Schema Readiness Notes

- `AgentInput` lives only in `src-rust/crates/query/src/agent_tool.rs:129-155`
- Parse site is local: `serde_json::from_value(input)` at `src-rust/crates/query/src/agent_tool.rs:221-225`
- Schema site is manual and local: `input_schema()` at `src-rust/crates/query/src/agent_tool.rs:175-218`
- `provider: Option<String>` assessment:
  - structurally isolated and backward-compatible
  - expected touch surface is primarily one file: `crates/query/src/agent_tool.rs`
  - no generated-schema machinery or duplicated helper layer was found
  - no dedicated `agent_tool.rs` tests were found
  - the built-in background auto-dream payload in `src-rust/crates/query/src/lib.rs:1568-1575` constructs agent input JSON, but it does not need a `provider` field unless later work wants explicit coverage
- Schema-risk note:
  - `AgentTool` is outside the `cc-tools` `all_tools()` list, so the generic `test_all_tools_have_valid_input_schema` in `src-rust/crates/tools/src/lib.rs:445-454` does not cover `AgentTool`
  - main M8-03 risk is manual schema drift, not architecture

## Workspace Baseline Snapshot

| Command | Result | Notes |
|---|---|---|
| `cd /home/jordi/claurst/src-rust && cargo fmt --all -- --check` | PASS | Clean |
| `cd /home/jordi/claurst/src-rust && cargo build --workspace` | PASS | Clean |
| `cd /home/jordi/claurst/src-rust && cargo test --workspace` | PASS | Warning-only output in existing unrelated areas (`claurst-commands`, `claurst-tui`, `claurst-query`, `claurst-core` tests), no failures |
| `cd /home/jordi/claurst/src-rust && cargo clippy --workspace --all-targets` | PASS | Warning-only output across existing unrelated areas (`core`, `api`, `commands`, `tui`, `query`, `cli`), no failures |

### Workspace Blocker Classification

- Hard blockers before M8 begins: none found
- Current baseline issues relevant to upcoming `M8-01..M8-03`: none demonstrated by the required commands
- Current warning noise: present, but appears pre-existing and unrelated to the narrow M8 seam tickets

## Hosted Ollama Invariant Outcome

- Verified current repo reality still preserves:
  - `normalize_ollama_api_base(...)` in `src-rust/crates/query/src/provider_resolution.rs:226-248`
  - hosted-root normalization tests in `src-rust/crates/query/src/provider_resolution.rs:314-347`
  - Ollama auth lookup via `AuthStore::load().api_key_for(ProviderId::OLLAMA)` in `src-rust/crates/query/src/provider_resolution.rs:229`
  - environment-first precedence for `AuthStore::api_key_for("ollama")` in `src-rust/crates/core/src/auth_store.rs:137-145`
  - env-precedence regression tests in `src-rust/crates/core/src/auth_store.rs:216-244`
- Threat assessment for upcoming M8 seam work:
  - no current repo reality threatens the hosted-Ollama baseline
  - the future risk area is `M8-04/M8-05` if they bypass these helpers or reintroduce direct provider-specific shortcuts instead of using the shared seam

`Hosted Ollama compatibility baseline preserved`

## Final Readiness Recommendation

`READY_FOR_M8_01_PREFLIGHT_WITH_NOTES`

## Notes / Risks

- `M8-01` itself looks low-risk because there are only three `ToolContext` construction sites and only two test fixtures need `None` fill-ins besides the `main.rs` production site.
- `M8-02` is not ownership-blocked, but it is not a trivial two-line drop-in at the current `ToolContext` line because `model_registry` is created later in startup.
- `M8-03` is isolated and ready, but its schema mirror is hand-maintained and not covered by the generic tool-schema smoke test.
- Full workspace validation is currently green; existing warnings should be treated as background noise rather than M8 blockers unless a later ticket widens scope to workspace cleanup.
- Read-only reconnaissance used parallel sub-agents for scans A/B/C; no source files were modified, staged, or committed.
