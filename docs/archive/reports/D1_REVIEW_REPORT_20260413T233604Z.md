# D1 Review Report

## D1 Scope Reviewed

TASK-M9-12 declares D1 complete. D1 is defined by the MPWO as Milestones 7, 8, and 9 combined: provider-resolution seam extraction (M7), worker propagation through the shared seam (M8), and validation/regression coverage proving the seam works end-to-end (M9). The MPWO contract for D1 completion at M9-12 requires:

1. `cargo fmt --all -- --check` passes
2. `cargo build --workspace` passes
3. `cargo test --workspace` passes
4. `cargo clippy --workspace --all-targets` passes
5. Manual smoke test: run Claurst with `--provider openai`, spawn an agent, confirm it uses OpenAI, **if possible in the test environment**
6. Document: "D1 is complete. Provider-resolution seam is landed. Workers inherit parent providers."
7. Do NOT start D2 work.

## Timestamp UTC

`2026-04-13T23:36:04Z`

## Branch Assessed

`feature/provider-resolution-seam` -- confirmed live via `git branch --show-current`.

HEAD is `6b362a0 TASK-M9-12 complete D1 provider-resolution seam validation and smoke gate`.

## Authority Reviewed

- `/home/jordi/claurst/AGENTS.md` -- reviewed in full
- `/home/jordi/claurst/docs/Current/MPWO_WORK_ORDER_PACK.md` -- reviewed: execution order summary, dependency graph, section 2A (Hosted Ollama invariant), TASK-M9-12 definition

## Evidence Reviewed

### Core D1 evidence (M9-12):

| Document | Verdict recorded |
|---|---|
| `TASK-M9-12_PREFLIGHT_REPORT_20260413T151224Z.md` | `READY-WITH-NOTES` |
| `TASK-M9-12_EXECUTION_RERUN_REPORT_20260413T153943Z.md` | `PASS / D1 COMPLETE WITH SMOKE-TEST ENV LIMITATION` |
| `TASK-M9-12_CLOSEOUT_REPORT_20260413T153943Z.md` | `REMEDIATED` -- four automated gates pass; smoke env-blocked |
| `TASK-M9-12_COMMIT_VERIFICATION_REPORT_20260413T165501Z.md` | `VERIFIED / D1 COMPLETE` |
| `TASK_CHILD_MAX_TOKENS_D1_INTERIM_CLOSEOUT_REPORT_20260413T163701Z.md` | `REMEDIATED` -- child max_tokens interim fallback, live smoke `PARENT_OK: CHILD_OK` confirmed |

### D1 dependency / closure evidence cross-checked:

| Document | Verdict recorded |
|---|---|
| `MILESTONE_M8_WRAPUP_CHECK_20260413T004403Z.md` | `M8 FORMALLY CLOSED` |
| `TASK-M9-02_COMMIT_VERIFICATION_REPORT_20260413T063215Z.md` | `VERIFIED` |
| `TASK-M9-03_COMMIT_VERIFICATION_REPORT_20260413T072303Z.md` | `VERIFIED` |
| `TASK-M9-07_COMMIT_VERIFICATION_REPORT_20260413T115639Z.md` | `VERIFIED` |
| `TASK-M9-08_COMMIT_VERIFICATION_REPORT_20260413T130029Z.md` | `VERIFIED` |
| `TASK-M9-09_COMMIT_VERIFICATION_REPORT_20260413T132904Z.md` | `VERIFIED` |
| `TASK-M9-10_EXECUTION_REPORT_20260413T140203Z.md` | `PASS / COMPLETE WITHOUT SOURCE CHANGE` |
| `TASK-M9-11_REMEDIATION_EXECUTION_REPORT_20260413T144105Z.md` | `REMEDIATED` |

## D1 Dependency Baseline Assessed

### M7 baseline:
- M7-07 closure is recorded in M8 wrapup as commit `b8cc827` with passing fmt/build/test/clippy.
- M7-07 is present in the live branch history (confirmed via `git log`).

### M8 baseline:
- M8 wrapup report (`MILESTONE_M8_WRAPUP_CHECK_20260413T004403Z.md`) formally closes M8 at commit `b5b6dd4` with all 11 M8 tickets verified closed.
- `b5b6dd4` is present in the live branch history (confirmed via `git log`).
- Dependency graph reconciliation in the M8 wrapup is thorough and addresses the M8-07/M8-08 ordering deviation.

### M9 ticket chain (M9-01 through M9-11):
- The preflight for M9-12 records all M9-01 through M9-11 tickets as closed or verified, with specific commit hashes for source-changing tickets.
- Commit chain confirmed live:
  - `662b29a` M9-02
  - `c28ef22` M9-03
  - `2f1f169` M9-04
  - `dfc4be4` M9-07
  - `63a8485` M9-08
  - `5e77652` M9-09
  - `af97a87` M9-11
- M9-01 (P1-P12 coverage audit), M9-05 (conflict detection), M9-06 (worker missing registry), M9-10 (no-key agent spawn) all closed as audit-only / no source change needed, per their respective execution reports and the M9-12 preflight confirmation.

**Assessment: Dependency baseline is sound.** All predecessor milestones and M9 tickets are accounted for, with commit hashes verifiable in live branch history.

## Validation Assessment

### Automated validation gate (4 commands):

The M9-12 commit verification report records that all four validation commands were run **twice** -- once pre-commit, once post-commit -- and passed both times:

| Command | Pre-commit | Post-commit |
|---|---|---|
| `cargo fmt --all -- --check` | PASS | PASS |
| `cargo build --workspace` | PASS | PASS |
| `cargo test --workspace` | PASS | PASS |
| `cargo clippy --workspace --all-targets` | PASS | PASS |

Non-blocking warnings were documented: unused import `Role` in `compact.rs`, unused variable `ctx` in `named_commands.rs`, non-snake-case test names in `prompt_input.rs`, and pre-existing lint debt across multiple crates. None of these escalated to errors and none are in scope for M9-12.

The M9-12 execution rerun report documents that two corrective patches were required during validation:
1. Five CLI call sites needed `Some(...)` wrapping for `run_query_loop()` signature change.
2. A shared test-only auth lock was needed to eliminate cross-test credential leakage.

Both corrections are documented, minimal, and within the scope of making the existing M9-11 changes compile and pass cleanly. They do not introduce new feature scope or D2 work.

**Assessment: Full automated validation gate is satisfied.** The 4-command suite passed on the committed HEAD, both before and after commit.

## Smoke / Live-Path Assessment

### MPWO contract for smoke:
> "Manual smoke test: run Claurst with `--provider openai`, spawn an agent, confirm it uses OpenAI (**if possible in the test environment**)."

### Evidence timeline:
1. **M9-12 execution rerun** (`20260413T153943Z`): Smoke classified as `NOT RUN` due to no OPENAI_API_KEY, no auth.json, network-restricted sandbox.
2. **M9-12 commit verification** (`20260413T165501Z`): Smoke attempted inside sandbox (failed on HTTP boundary), then attempted outside sandbox (reached OpenAI but key was revoked). User confirmed: "the key has been revoked. but assume it would work otherwise."
3. **TASK_CHILD_MAX_TOKENS interim** (`20260413T163701Z`): **A prior live smoke succeeded** with final output `PARENT_OK: CHILD_OK` on the same intended patch shape. This was on an earlier run where the key was still valid and the environment had network access.

### Analysis:

The MPWO smoke clause includes the explicit qualifier "if possible in the test environment." This makes the smoke test conditional, not mandatory. The evidence shows:

- The smoke infrastructure exists and is wired (CLI `--provider` flag, AgentTool, team swarm runner).
- Automated test coverage exists proving the OpenAI routing path works: `agent_explicit_provider_routes_to_openai_provider`, `agent_parent_inherits_provider_openai_dispatch`, `teamcreate_mixed_providers_per_agent_dispatch`.
- A prior authoritative live smoke on the same intended patch shape did succeed with `PARENT_OK: CHILD_OK`.
- The commit-verification session's smoke failure is attributable to a revoked API key, not a code regression.

**Assessment: Smoke evidence is acceptable under the MPWO wording.** The conditional clause explicitly accounts for environment limitations. The combination of (a) a prior successful live smoke on the same patch shape, (b) automated test coverage of the routing path, and (c) a clear environmental explanation for the final-session smoke failure provides sufficient basis. This is not a gap in code coverage; it is an environment-level authentication limitation.

## Hosted-Ollama Invariant Assessment

MPWO section 2A requires all M7-M12 seam tickets to preserve:
1. `normalize_ollama_api_base(...)` -- **present** in `provider_resolution.rs` (confirmed via grep)
2. `AuthStore::load().api_key_for(ProviderId::OLLAMA)` -- **present** in `provider_resolution.rs:229` (confirmed via grep)
3. Environment-first precedence for `AuthStore::api_key_for("ollama")` -- **present** in `auth_store.rs` tests (confirmed via grep)

Every reviewed M9 report explicitly states "Hosted Ollama compatibility baseline preserved." The M8 wrapup confirms the same across all M8 tickets. The M9-12 commit verification report states "no hosted-Ollama reopening."

The M9-12 commit diff (`4 files changed, 38 insertions(+), 40 deletions(-)`) touches `main.rs`, `agent_tool.rs`, `lib.rs`, and `provider_resolution.rs`. The commit verification audit confirms the diff contains only:
- `Some(...)` wrapping for the legacy client parameter
- Shared test-only auth-lock hookup
- Formatting-only remediation
- Child max_tokens interim fallback

None of these changes interact with Ollama URL normalization, Ollama auth lookup, or environment-first precedence.

**Assessment: Hosted-Ollama non-regression invariant is preserved.** Both grep-verified code presence and report-chain attestation confirm this.

## Findings

### No blockers found.

### Observations (non-blocking):

1. **Corrective patches in M9-12**: The validation-only ticket required two source corrections (Some-wrapping and shared auth lock). This is documented and justified -- M9-11 changed the `run_query_loop` signature, and the CLI call sites needed updating. The shared auth lock fixed a pre-existing test nondeterminism issue. Both are within the spirit of M9-12 as a "make everything pass" gate.

2. **Child max_tokens interim fallback**: The `TASK_CHILD_MAX_TOKENS_D1_INTERIM_CLOSEOUT_REPORT` documents a `4096` hardcoded fallback for spawned child agents. This is explicitly marked as D1-safe interim, not final policy. It does not widen D1 scope because it does not introduce D2 concepts (no trust domains, no fallback resolution, no health cache).

3. **Report artifacts remain untracked**: The reports under `docs/archive/reports/` are untracked git files. Per the task instructions: "do not fail solely because historical report artifacts are untracked if the review basis is otherwise explicit and evidence-backed." The evidence basis is explicit and every report was readable and consistent.

4. **Non-blocking lint warnings**: Pre-existing unused imports and non-snake-case test names are documented but not in scope for M9-12 or D1.

5. **Single `.gitignore` unstaged change**: Live `git diff --name-only` shows only `.gitignore` as unstaged, which is unrelated to D1 scope.

## Verdict

**PASS -- D1 REVIEW-ACCEPTED**

D1 is complete as defined by the MPWO contract. The evidence supports all of the following:

- All M7 tickets (seam extraction) are closed and committed.
- All M8 tickets (worker propagation) are closed and committed, with M8 formally closed.
- All M9 tickets (M9-01 through M9-11 validation/coverage, M9-12 full regression) are closed and committed.
- The four required automated validation commands pass on the committed HEAD.
- The manual smoke test was environment-blocked due to a revoked API key, which is acceptable under the MPWO's "if possible in the test environment" qualifier. Prior live smoke evidence on the same patch shape exists (`PARENT_OK: CHILD_OK`).
- The hosted-Ollama non-regression invariant is preserved.
- No D2 / M10 / M11 / M12 work was started.
- The documented declaration "D1 is complete. Provider-resolution seam is landed. Workers inherit parent providers." is grounded in the evidence.

## Ready for Next Phase

**Yes.**

D1 is review-accepted. The next phase per the MPWO is M10 (D2 planning). M10 is a planning-only milestone and was explicitly excluded from the MPWO execution scope ("Not included: M10 (planning-only)"). D2 implementation begins at M11 after M10 planning is reviewed and accepted.

## Exact Blockers or Residual Notes

- **Blockers:** None.
- **Residual notes:**
  - The child max_tokens interim fallback (`4096`) is a D1-safe stopgap. Final parent/child token policy is future work.
  - Pre-existing lint warnings remain across several crates. These are not D1 scope.
  - Report artifacts should ideally be tracked in git for auditability, but this is a process preference, not a D1 blocker.
